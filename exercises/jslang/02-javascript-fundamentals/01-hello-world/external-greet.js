// This file is loaded by the step 2 exercises via <script src="...">.
// It defines a function that can be called from later script blocks.

function greet(name) {
  return "Hello, " + name + "!";
}

window.__externalGreetLoaded = true;
